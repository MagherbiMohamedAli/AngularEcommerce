import { SaleHighlightDirective } from './sale-highlight.directive';

describe('SaleHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new SaleHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
